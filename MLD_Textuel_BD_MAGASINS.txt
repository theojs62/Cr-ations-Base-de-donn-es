CLIENT_ = (num_cl INT, nom_cl VARCHAR(50), prenom_cl VARCHAR(50), email_cl VARCHAR(50));
MAGASIN_ = (num_mag__ INT, num_rue_mag VARCHAR(50), nom_rue_mag VARCHAR(50), code_p_mag VARCHAR(50), ville_mag VARCHAR(50), pays_mag VARCHAR(50), sup_stock_mag DECIMAL(15,2));
FOURNISSEUR_ = (num_fourn INT, nom_fourn VARCHAR(50), num_rue_fourn VARCHAR(50), nom_rue_fourn VARCHAR(50), code_p_fourn VARCHAR(50), ville_fourn VARCHAR(50), pays_fourn VARCHAR(50));
EMPLOYE_ = (num_emp INT, nom_emp VARCHAR(50), prenom_emp VARCHAR(50), ddn_emp DATE, #num_mag__);
ACHAT = (num_achat INT, date_achat DATE, #num_cl, #num_mag__);
CARTE_FIDELITE = (num_carte_f INT, date_creation_f DATE, #num_cl, #num_mag__);
PRODUIT_ = (num_prod_ INT, designation_prod_ VARCHAR(50), prix_u_prod DECIMAL(15,2), #num_fourn);
CONTIENT_ = (#num_achat, #num_prod_, quantite_achat DECIMAL(15,2));
DIRIGE_ = (#num_mag__, #num_emp);
